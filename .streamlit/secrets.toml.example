# Streamlit secrets TEMPLATE - this file is COMMITTED to git.
# Never put a real key here. Copy it to secrets.toml (git-ignored) instead,
# or paste the contents into your host's secrets/environment settings.

GEMINI_API_KEY = "your-generative-ai-key-here"

# Optional: override the Admin module password (defaults to EdTech@123)
# LADA_ADMIN_PASSWORD = "choose-a-strong-password"
