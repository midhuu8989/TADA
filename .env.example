# Copy to .env and fill in. The key can also be entered in the Admin module,
# which stores it encrypted at rest instead of on disk in plaintext.
GEMINI_API_KEY=

# Optional overrides
# LADA_ADMIN_PASSWORD=EdTech@123
# LADA_OFFLINE=0
